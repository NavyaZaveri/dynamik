@global val hello = "hello";
class Data(x, y) {
    fn get_x() {  return this.x;}
    fn get_y() { return this.y;}
}

val d = Data(10,20);
print(d.get_x() + d.get_y());

class Thing(data) {
    fn blah() {
        print(data.get_x() + data.get_y());
     }
}

val t = Thing(d);
t.blah();
t.data.x = 100;
t.blah();
