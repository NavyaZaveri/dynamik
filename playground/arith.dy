/*
fn add(x, y) { return x + y;}
fn mul(x, y) { return x * y;}
fn add_and_mul(x, y) {
    val hello = "helllooo";

    fn sub(x,y) {
       return x-y;
    }
   val wtf = sub(x,y);
    return wtf;
} */


fn cc(x){
val y=4;
return x;
}
print cc(3);


