class Math() {
    fn abs(x) {
        if (x<=0) { return -x;}
        return x;
     }

     fn add(x,y) { return x+y;}
     fn sub(x,y) {return x-y;}
}

val math = Math();
val pos_one = math.abs(-1);
print(pos_one);
